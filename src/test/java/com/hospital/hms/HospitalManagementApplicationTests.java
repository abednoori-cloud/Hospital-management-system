package com.hospital.hms;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

@SpringBootTest
class HospitalManagementApplicationTests {

    @Test
    void contextLoads() {
        // Verifies that the full Spring application context
        // (controllers, services, repositories, security config)
        // starts up successfully with the default H2 profile.
    }
}
